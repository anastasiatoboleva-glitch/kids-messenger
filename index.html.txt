<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Детский мессенджер</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .chat-container {
            width: 100%;
            max-width: 500px;
            height: 90vh;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }
        
        .chat-header {
            background: #FF6600;
            color: white;
            padding: 15px;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
        }
        
        .messages {
            flex: 1;
            overflow-y: auto;
            padding: 15px;
            background: #f5f5f5;
        }
        
        .message {
            margin-bottom: 10px;
            display: flex;
            flex-direction: column;
        }
        
        .message.user {
            align-items: flex-end;
        }
        
        .message.bot {
            align-items: flex-start;
        }
        
        .message-bubble {
            max-width: 70%;
            padding: 10px 15px;
            border-radius: 18px;
            word-wrap: break-word;
        }
        
        .user .message-bubble {
            background: #FF6600;
            color: white;
            border-bottom-right-radius: 5px;
        }
        
        .bot .message-bubble {
            background: white;
            color: #333;
            border-bottom-left-radius: 5px;
            box-shadow: 0 1px 2px rgba(0,0,0,0.1);
        }
        
        .message-time {
            font-size: 11px;
            color: #999;
            margin-top: 5px;
        }
        
        .input-area {
            padding: 15px;
            background: white;
            border-top: 1px solid #eee;
            display: flex;
            gap: 10px;
        }
        
        .input-area input {
            flex: 1;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 25px;
            outline: none;
            font-size: 16px;
        }
        
        .input-area input:focus {
            border-color: #FF6600;
        }
        
        .input-area button {
            background: #FF6600;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 16px;
        }
        
        .input-area button:active {
            transform: scale(0.95);
        }
        
        .emoji-picker {
            display: none;
            grid-template-columns: repeat(6, 1fr);
            gap: 10px;
            padding: 10px;
            background: white;
            border-top: 1px solid #eee;
        }
        
        .emoji-picker span {
            font-size: 28px;
            text-align: center;
            cursor: pointer;
            padding: 5px;
        }
        
        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background: #f00;
            border-radius: 50%;
            pointer-events: none;
            animation: fall 2s linear forwards;
        }
        
        @keyframes fall {
            to {
                transform: translateY(100vh) rotate(360deg);
                opacity: 0;
            }
        }
        
        .toolbar {
            display: flex;
            gap: 10px;
            padding: 10px 15px;
            background: white;


border-bottom: 1px solid #eee;
        }
        
        .toolbar button {
            background: none;
            border: none;
            font-size: 20px;
            cursor: pointer;
            padding: 5px;
        }
        
        .login-screen {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }
        
        .login-box {
            background: white;
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            width: 90%;
            max-width: 300px;
        }
        
        .login-box input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 25px;
            font-size: 16px;
        }
        
        .login-box button {
            background: #FF6600;
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 25px;
            font-size: 16px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="login-screen" id="loginScreen">
        <div class="login-box">
            <h2>👋 Привет!</h2>
            <p>Введите своё имя</p>
            <input type="text" id="userName" placeholder="Твоё имя" maxlength="20">
            <button onclick="login()">Войти</button>
        </div>
    </div>
    
    <div class="chat-container" id="chatContainer" style="display: none;">
        <div class="chat-header">
            🎈 Детский мессенджер 🎈
        </div>
        <div class="toolbar">
            <button onclick="showEmoji()">😊</button>
            <button onclick="showConfetti()">🎉</button>
            <button onclick="clearChat()">🗑️</button>
        </div>
        <div class="messages" id="messages">
            <div class="message bot">
                <div class="message-bubble">
                    Привет! Я твой помощник. Здесь можно общаться и веселиться! 🎈
                </div>
                <div class="message-time">Только что</div>
            </div>
        </div>
        <div class="emoji-picker" id="emojiPicker">
            <span>😀</span> <span>😂</span> <span>🥰</span> <span>😎</span> <span>🎉</span> <span>❤️</span>
            <span>👍</span> <span>🎈</span> <span>🐶</span> <span>🐱</span> <span>🌟</span> <span>🎵</span>
        </div>
        <div class="input-area">
            <input type="text" id="messageInput" placeholder="Напиши сообщение..." onkeypress="if(event.key === 'Enter') sendMessage()">
            <button onclick="sendMessage()">📤</button>
        </div>
    </div>
    
    <script>
        let currentUser = '';
        
        function login() {
            const name = document.getElementById('userName').value.trim();
            if (name === '') {
                alert('Пожалуйста, введите имя!');
                return;
            }
            currentUser = name;
            document.getElementById('loginScreen').style.display = 'none';
            document.getElementById('chatContainer').style.display = 'flex';
            addMessage('bot', `Привет, ${name}! Рад тебя видеть! 🎉`);
        }
        
        function addMessage(sender, text) {
            const messagesDiv = document.getElementById('messages');
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${sender}`;
            
            const now = new Date();
            const time = `${now.getHours().toString().padStart(2,'0')}:${now.getMinutes().toString().padStart(2,'0')}`;
            
            messageDiv.innerHTML = `
                <div class="message-bubble">${text}</div>
                <div class="message-time">${time}</div>
            `;
            
            messagesDiv.appendChild(messageDiv);
            messagesDiv.scrollTop = messagesDiv.scrollHeig


ht;
        }
        
        function sendMessage() {
            const input = document.getElementById('messageInput');
            const text = input.value.trim();
            if (text === '') return;
            
            addMessage('user', text);
            input.value = '';
            
            setTimeout(() => {
                let reply = '';
                if (text.includes('привет') || text.includes('здравствуй')) {
                    reply = `Привет, ${currentUser}! Как дела? 😊`;
                } else if (text.includes('как дела')) {
                    reply = 'У меня всё отлично! А у тебя? 🎈';
                } else if (text.includes('спасибо')) {
                    reply = 'Пожалуйста! Всегда рад помочь! 🌟';
                } else if (text.includes('пока')) {
                    reply = 'Пока! Приходи ещё! 👋';
                } else {
                    reply = `Интересно! Расскажи ещё, ${currentUser}! 😊`;
                }
                addMessage('bot', reply);
            }, 500);
        }
        
        function showEmoji() {
            const picker = document.getElementById('emojiPicker');
            picker.style.display = picker.style.display === 'none' ? 'grid' : 'none';
            
            const emojis = document.querySelectorAll('#emojiPicker span');
            emojis.forEach(emoji => {
                emoji.onclick = function() {
                    const input = document.getElementById('messageInput');
                    input.value += this.textContent;
                    picker.style.display = 'none';
                };
            });
        }
        
        function showConfetti() {
            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * window.innerWidth + 'px';
                confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 70%, 60%)`;
                confetti.style.width = Math.random() * 8 + 4 + 'px';
                confetti.style.height = confetti.style.width;
                document.body.appendChild(confetti);
                setTimeout(() => confetti.remove(), 2000);
            }
        }
        
        function clearChat() {
            const messagesDiv = document.getElementById('messages');
            messagesDiv.innerHTML = '';
            addMessage('bot', 'Чат очищен! Напиши что-нибудь 😊');
        }
        
        // Для мобильных устройств
        window.addEventListener('touchstart', function() {});
    </script>
</body>
</html>